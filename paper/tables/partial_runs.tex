The following runs were still filling up when the results were frozen and are excluded from every table, average and fitted line in this paper, on a pre-set threshold of 30 scored items: SmolLM2-1.7B on ToolShed (tool calls) ($n=13$). Their raw scores are in the release.
