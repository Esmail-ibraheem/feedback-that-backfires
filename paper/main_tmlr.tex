% TMLR submission build.
%
%   tectonic -X compile main_tmlr.tex
%
% This is a wrapper, not a fork: every section, table, figure and generated
% number is the same file the two-column build in main.tex uses. Only the page
% furniture differs. Anything that needs to say something different in the two
% layouts belongs in a \newcommand here, not in a duplicated section file.
%
% `\usepackage{tmlr}` with no options is the anonymous form: the class replaces
% the author block with "Anonymous authors / Paper under double-blind review"
% and sets the "Under review as submission to TMLR" running head. Do NOT add
% [accepted] or [preprint] for a submission.
\documentclass{article}

\usepackage{tmlr/tmlr}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{textcomp}   % \textminus: a real minus in tables, not a hyphen
\usepackage{times}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{makecell}
\usepackage{float}      % [H]: appendix reference tables stay under their heading
\usepackage{xcolor}
\usepackage{microtype}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{natbib}
\usepackage[hidelinks]{hyperref}
\usepackage{url}

\captionsetup{font=small,labelfont=bf,skip=4pt}
\setlength{\abovecaptionskip}{4pt}
\setlength{\belowcaptionskip}{2pt}

% Compact confidence-interval notation used by every generated table.
\newcommand{\ci}[2]{{\scriptsize\,[#1,\,#2]}}
% TMLR's text block is 6.5in against the 6.9in the two-column build gives a
% full-width float, so the wide tables step down one size here.
\newcommand{\tblfont}{\scriptsize}
\newlength{\tblsep}\setlength{\tblsep}{3pt}
\newcommand{\tblfontsmall}{\scriptsize}
\newlength{\tblsepsmall}\setlength{\tblsepsmall}{2pt}
% Shorthands for the quantities defined in Section 3.
\newcommand{\afail}{a^{\times}}
\newcommand{\agold}{a^{\checkmark}}
\newcommand{\Gain}{G}
\newcommand{\copyterm}{\Delta_{\mathrm{copy}}}
\newcommand{\semterm}{\Delta_{\mathrm{sem}}}
\newcommand{\ctxpre}{C}
\newcommand{\todo}[1]{\textcolor{red}{\textbf{[#1]}}}

% Every inline number in this manuscript is a macro generated from results/raw
% by scripts/make_facts.py. Anything not yet measured expands to a visible
% placeholder rather than to a plausible-looking number.
\input{tables/facts}

% No manual break: TMLR's title box is 6.5in wide and wraps this cleanly on its
% own. A forced `\\` here strands a single word on the second line.
\title{Feedback That Backfires: Why Small Language Model Agents Repeat
the Call They Just Watched Fail}

% Ignored while anonymous; kept so the accepted build needs no edit here.
\author{\name Esmail Gumaan \email mokevara.strawhat@gmail.com}

\begin{document}

\maketitle

\begin{abstract}
\input{sections/abstract_body}
\end{abstract}

\input{sections/introduction}
\input{sections/related}
\input{sections/problem}
\input{sections/method}
\input{sections/setup}
\input{sections/results}
\input{sections/analysis}
\input{sections/agent}
\input{sections/efficiency}
\input{sections/failure}
\input{sections/limitations}
\input{sections/conclusion}

\bibliographystyle{tmlr/tmlr}
\bibliography{references}

\appendix
\input{sections/appendix}

\end{document}
